query ProjectTasks($id: Float!) {
  project(projectId: $id) {
    id
    name
    description
    tasks {
      id
      name
      description
      createdAt
      dueDate
      completed
    }
  }
}
