query Roles($admin: Boolean, $permission: String) {
  roles(filter: { admin: $admin, permission: $permission }) {
    id
    name
    slug
    system
    admin
    teacher
    student
    permissions {
      name
      slug
    }
  }
}
