query Project($id: Float!) {
  project(projectId: $id) {
    id
    name
    description
    createdAt
    tasks {
      id
      name
      description
      createdAt
      dueDate
      completed
    }
    classifications {
      id
      createdAt
      mark
      note
      user {
        id
        name
      }
    }
    owner {
      id
      name
    }
    users {
      id
      name
    }
    supervisor {
      id
      name
    }
  }
}
