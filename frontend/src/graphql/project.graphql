query Project($id: Float) {
  project(filter: { id: $id }) {
    id
    name
    description
    createdAt
    tasks {
      id
      name
      description
      createdAt
      dueDate
      completed
    }
    classifications {
      id
      createdAt
      mark
      note
      user {
        id
        name
      }
    }
    users {
      id
      name
    }
    supervisor {
      id
      name
    }
  }
}
